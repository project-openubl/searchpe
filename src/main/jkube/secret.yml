stringData:
  QUARKUS_DATASOURCE_USERNAME: ${username}
  QUARKUS_DATASOURCE_PASSWORD: ${password}
  QUARKUS_DATASOURCE_JDBC_URL: ${jdbcUrl}
  QUARKUS_OIDC_AUTH_SERVER_URL: ${oidcAuthServerUrl}
  QUARKUS_OIDC_CLIENT_ID: ${oidcClientId}
  QUARKUS_OIDC_CREDENTIALS_SECRET: ${oidcCredentialsSecret}
type: Opaque
