package io.searchpe.models.jpa;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeJpaVersionProviderIT extends JpaVersionProviderTest {

    // Execute the same tests but in native mode.
}